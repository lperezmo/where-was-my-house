{
  "name": "where-was-my-house",
  "private": true,
  "description": "Enter an address, get a 540-million-year biography of that ground",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "tsc --noEmit && bun run --bun vite build",
    "preview": "bun run --bun vite preview",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "coastlines": "bun run scripts/build-coastlines.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "happy-dom": "^20.11.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5"
  }
}
